# 283. Move Zeroes / Mover Ceros

## English

Given an integer array `nums`, move all `0`'s to the end of it while maintaining the relative order of the non-zero elements.

Note that you must do this in-place without making a copy of the array.

### Examples

#### Example 1
```text
Input: nums = [0,1,0,3,12]
Output: [1,3,12,0,0]
```

#### Example 2
```text
Input: nums = [0]
Output: [0]
```

### Constraints

- 1 <= nums.length <= 10^4
- -2^31 <= nums[i] <= 2^31 - 1

### Follow-up

Could you minimize the total number of operations done?

---

## Espanol

Dado un arreglo de enteros `nums`, mueve todos los `0` al final del mismo manteniendo el orden relativo de los elementos distintos de cero.

Ten en cuenta que debes hacerlo in-place sin hacer una copia del arreglo.

### Ejemplos

#### Ejemplo 1
```text
Entrada: nums = [0,1,0,3,12]
Salida: [1,3,12,0,0]
```

#### Ejemplo 2
```text
Entrada: nums = [0]
Salida: [0]
```

### Restricciones

- 1 <= nums.length <= 10^4
- -2^31 <= nums[i] <= 2^31 - 1

### Desafio adicional

Podrias minimizar el numero total de operaciones realizadas?
