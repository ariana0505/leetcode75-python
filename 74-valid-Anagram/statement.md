# 74. Valid Anagram / Anagrama Valido

## English

Given two strings `s` and `t`, return `true` if `t` is an **anagram** of `s`, and `false` otherwise.

### Examples

#### Example 1
```text
Input: s = "anagram", t = "nagaram"
Output: true
```

#### Example 2
```text
Input: s = "rat", t = "car"
Output: false
```

### Constraints

- 1 <= s.length, t.length <= 5 * 10^4
- `s` and `t` consist of **lowercase English letters**

### Follow-up

What if the inputs contain **Unicode characters**?
How would you adapt your solution to handle such a case?

---

## Espanol

Dadas dos cadenas `s` y `t`, devuelve `true` si `t` es un **anagrama** de `s`, y `false` en caso contrario.

### Ejemplos

#### Ejemplo 1
```text
Entrada: s = "anagram", t = "nagaram"
Salida: true
```

#### Ejemplo 2
```text
Entrada: s = "rat", t = "car"
Salida: false
```

### Restricciones

- 1 <= s.length, t.length <= 5 * 10^4
- `s` y `t` consisten unicamente de **letras minusculas del alfabeto ingles**

### Desafio adicional

Que pasaria si las entradas contienen **caracteres Unicode**?
Como adaptarias tu solucion para manejar ese caso?
