# 39. Reverse Linked List / Invertir Lista Enlazada

## English

Given the `head` of a singly linked list, reverse the list, and return the reversed list.

### Examples

#### Example 1
```text
Input: head = [1,2,3,4,5]
Output: [5,4,3,2,1]
```

#### Example 2
```text
Input: head = [1,2]
Output: [2,1]
```

#### Example 3
```text
Input: head = []
Output: []
```

### Constraints

- The number of nodes in the list is the range [0, 5000].
- -5000 <= Node.val <= 5000

**Follow up:** A linked list can be reversed either iteratively or recursively. Could you implement both?

---

## Espanol

Dada la cabeza `head` de una lista enlazada simple, invierte la lista y devuelve la lista invertida.

### Ejemplos

#### Ejemplo 1
```text
Entrada: head = [1,2,3,4,5]
Salida: [5,4,3,2,1]
```

#### Ejemplo 2
```text
Entrada: head = [1,2]
Salida: [2,1]
```

#### Ejemplo 3
```text
Entrada: head = []
Salida: []
```

### Restricciones

- El numero de nodos de la lista esta en el rango [0, 5000].
- -5000 <= Node.val <= 5000

**Seguimiento:** Una lista enlazada se puede invertir de forma iterativa o recursiva. Podrias implementar ambas?
