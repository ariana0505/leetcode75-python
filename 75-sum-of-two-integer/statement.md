## Sum of Two Integers (English)

Given two integers `a` and `b`, return the **sum of the two integers** without using the operators `+` and `-`.

### Example 1
**Input:** `a = 1`, `b = 2`  
**Output:** `3`

### Example 2
**Input:** `a = 2`, `b = 3`  
**Output:** `5`

### Constraints
- `-1000 <= a, b <= 1000`

---

## Suma de Dos Enteros (Español)

Dados dos enteros `a` y `b`, devuelve la **suma de ambos enteros** sin usar los operadores `+` y `-`.

### Ejemplo 1
**Entrada:** `a = 1`, `b = 2`  
**Salida:** `3`

### Ejemplo 2
**Entrada:** `a = 2`, `b = 3`  
**Salida:** `5`

### Restricciones
- `-1000 <= a, b <= 1000`
