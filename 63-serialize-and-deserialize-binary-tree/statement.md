# 63. Serialize and Deserialize Binary Tree / Serializar y Deserializar un Arbol Binario

## English

Serialization is the process of converting a data structure or object into a sequence of bits so that it can be stored in a file or memory buffer, or transmitted across a network connection link to be reconstructed later in the same or another computer environment.

Design an algorithm to serialize and deserialize a binary tree. There is no restriction on how your serialization/deserialization algorithm should work. You just need to ensure that a binary tree can be serialized to a string and this string can be deserialized to the original tree structure.

### Examples

#### Example 1
```text
Input: root = [1,2,3,null,null,4,5]
Output: [1,2,3,null,null,4,5]
```

#### Example 2
```text
Input: root = []
Output: []
```

### Constraints

- The number of nodes in the tree is in the range [0, 10^4].
- -1000 <= Node.val <= 1000

---

## Espanol

La serializacion es el proceso de convertir una estructura de datos u objeto en una secuencia de bits para que pueda almacenarse en un archivo o buffer de memoria, o transmitirse a traves de una conexion de red para ser reconstruida despues en el mismo entorno de computo u otro.

Disena un algoritmo para serializar y deserializar un arbol binario. No hay restriccion sobre como debe funcionar tu algoritmo de serializacion/deserializacion. Solo necesitas asegurarte de que un arbol binario pueda serializarse a un string y que ese string pueda deserializarse a la estructura de arbol original.

### Ejemplos

#### Ejemplo 1
```text
Entrada: root = [1,2,3,null,null,4,5]
Salida: [1,2,3,null,null,4,5]
```

#### Ejemplo 2
```text
Entrada: root = []
Salida: []
```

### Restricciones

- El numero de nodos en el arbol esta en el rango [0, 10^4].
- -1000 <= Node.val <= 1000
